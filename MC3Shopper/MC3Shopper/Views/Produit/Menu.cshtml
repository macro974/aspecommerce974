@model List<String>
<div class="col-sm-4">
    <ul class="multi-column-dropdown">
        @for (int i = 0; i < 9; i++)
        {

            <li>@Html.ActionLink(@Model[i], "Category", new { Sate02 = @Model[i] })</li>
        }
    </ul>
</div><div class="col-sm-4">
    <ul class="multi-column-dropdown">
        @for (int i =10; i < 19; i++)
        {

            <li>@Html.ActionLink(@Model[i], "Category", new { Sate02 = @Model[i] })</li>
        }
    </ul>
</div><div class="col-sm-4">
    <ul class="multi-column-dropdown">
        @for (int i = 20; i < 29; i++)
        {

            <li>@Html.ActionLink(@Model[i], "Category", new { Sate02 = @Model[i] })</li>
        }
    </ul>
</div>