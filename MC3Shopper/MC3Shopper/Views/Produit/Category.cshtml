@using System.Globalization
@{
    ViewBag.Title = HttpUtility.UrlDecode(ViewBag.category.Replace('!', '%'));
    Layout = "~/Views/Shared/_Layout.cshtml";
    IFormatProvider culture = new CultureInfo("fr-FR", true);
}

<div class="page-header">
    <h2 id="titre-cat">@HttpUtility.UrlDecode(@ViewBag.category.Replace('_', '/'))</h2>
</div>
<div class=" marginer-bottom">
    <div class="col-md-12">
        <div class="pull-left">@Html.Action("_fam", new { Stat02 = @ViewBag.category })</div>
        <div class="pull-right">
            <a href="#" class="btn  btn-primary " id="export_csv">Exporter au format CSV</a>
        </div>
    </div>
</div>
@{Html.RenderAction("_getListProduct", "Produit", new { Stat02 = @ViewBag.Category });}